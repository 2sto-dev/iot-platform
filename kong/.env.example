# Copy to .env on the Kong VM and fill in the real secret.
# Loaded by Kong via vault reference `{vault://env/kong-jwt-secret}` in kong.yaml.
# Must be identical with JWT_SECRET in django-bakend/.env and go-iot-platform/.env.
KONG_JWT_SECRET=<paste-the-shared-jwt-secret-here>
